/*input

*/
#include<bits/stdc++.h>
using namespace std;
#define int long long
#define ii pair<int,int>
#define mapi map<int,int>
#define M 1000000007
#define pi 3.141592653589793
#define fast ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define pb push_back
#define ppb pop_back
#define pf push_front
#define ppf pop_front
#define re(n) for(int i=0; i<n; i++)
#define rre(n) for(int i=n-1; i>-1; i--) 
#define rep(i,n) for(int i=0; i<n; i++)
#define lop(i,a,b) for(int i=a; i<b; i++)
#define in(x) int x; cin>>x;
#define fr first
#define sc second
#define time cout << "\nTime elapsed: " << 1.0 * clock() / CLOCKS_PER_SEC << " s.\n";
#define all(x) x.begin(),x.end()
#define mem0(x) memset(x, 0, sizeof(x))
#define mem1(x) memset(x, -1, sizeof(x))
#define sa(x) std::sort(x.begin(),x.end())
#define sd(x) std::sort(x.begin(),x.end(),greater<int>())
#define trav(a) for(auto &x:a)
///////////////////////////
//Returns index of first element not less than key and more than key respectively

#define lb(data,key) lower_bound(all(data),key) - data.begin();
#define up(data,key) upper_bound(all(data),key) - data.begin();
#define fnd(data,key) find(all(data),key) - data.begin();
////////////////////////////
signed solve()
{
	cout<<"Hello COPS";
	return 0;
}

signed main()
{
	fast
	int t=1;
	//cin>>t;
	while(t--) solve();

	return 0;
}